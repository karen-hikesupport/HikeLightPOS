<?xml version="1.0" encoding="UTF-8"?>
<local:BaseContentPage 
		xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
		x:Class="HikePOS.SettingUpOutletPage" 
		xmlns:local="clr-namespace:HikePOS"
        xmlns:vm="clr-namespace:HikePOS.ViewModels"
        x:TypeArguments="vm:SettingUpOutletViewModel"
		xmlns:resources="clr-namespace:HikePOS.Resources" 
		xmlns:Localize="clr-namespace:HikePOS;assembly=HikePOS"
        x:DataType="vm:SettingUpOutletViewModel"
        Title="{Binding Title}">
	<ContentPage.Content>
		<Grid RowSpacing="0" ColumnSpacing="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="20*" />
                        <RowDefinition Height="80*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
            <StackLayout Grid.Row="0" Grid.Column="0" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                <Image Source="{x:Static resources:AppImages.HikeLogo}" HeightRequest="150" Aspect="AspectFit" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
            </StackLayout>
            <ContentView Grid.Row="1" Grid.Column="0" BackgroundColor="{x:Static resources:AppColors.LoginBackgroundColor}"></ContentView>
             <Grid Grid.Row="1" Grid.Column="0" RowSpacing="2" VerticalOptions="Center" HorizontalOptions="FillAndExpand">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <ActivityIndicator Grid.Row="0" Grid.Column="0" IsRunning="{Binding IsBusy}" Style="{x:Static resources:Styles.ActivityIndicatorStyle}" >
                        <ActivityIndicator.Scale>
                        <OnPlatform x:TypeArguments="x:Double">
                             <On Platform="iOS" Value="2" />
                             <On Platform="Android" Value="1" />
                        </OnPlatform>
                        </ActivityIndicator.Scale>
                    </ActivityIndicator>
                    <Label Grid.Row="1" Grid.Column="0" Text="{Localize:Translate Progress0_Text}" Style="{x:Static resources:Styles.WaitProgressTitleLabelStyle}" />
                    <Label Grid.Row="2" Grid.Column="0" x:Name="ProgressSttusLabel" Text="{Binding ProcessTitle}" Style="{x:Static resources:Styles.ProgressTitleLabelStyle}" />
                </Grid>
        </Grid>

    </ContentPage.Content>
</local:BaseContentPage>
